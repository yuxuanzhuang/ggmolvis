name: ggmolvis-test
channels:
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - python
  - pip
  - pydantic

  # MDAnalysis
  - MDAnalysis

  # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov

  # Pip-only installs
  - pip:
    # get from github
    - git+https://github.com/BradyAJohnston/MolecularNodes.git@v4.2.5
    - bpy
  #  - codecov

