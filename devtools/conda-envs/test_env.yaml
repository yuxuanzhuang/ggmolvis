name: ggmolvis-test
channels:
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - python
  - pip
  - molecularnodes
  - pydantic
  - bpy

  # MDAnalysis
  - MDAnalysis

  # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov

  # Pip-only installs
  #- pip:
  #  - codecov

